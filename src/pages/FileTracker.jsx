import SectionStub from "../components/SectionStub";

export default function FileTracker() {
  return (
    <SectionStub
      title="File tracker"
      note="Real 29-item checklist + compliance reports get ported in here, reusing employee-tracker-v2's existing logic — reading employee identity from the shared roster instead of its own import."
    />
  );
}
