import SectionStub from "../components/SectionStub";

export default function HrTimeline() {
  return (
    <SectionStub
      title="HR timeline"
      note="Real probation/milestone/notes tracking gets ported in here from hr-timeline-tracker, keeping its extra HR-only fields (joining date, phone, department, manager) but linking to the shared roster by ID instead of storing its own name field."
    />
  );
}
