import SectionStub from "../components/SectionStub";

export default function LabelGenerator() {
  return (
    <SectionStub
      title="Label generator"
      note="Real label creation, QR generation, and print flow get ported in here from label-generator-v3.4, moving from localStorage to the shared Firestore roster + a new /labels collection."
    />
  );
}
