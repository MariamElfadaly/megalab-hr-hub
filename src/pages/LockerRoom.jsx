import SectionStub from "../components/SectionStub";

export default function LockerRoom() {
  return (
    <SectionStub
      title="Locker room"
      note="Real locker grid + drag-and-drop assignment gets ported in here from locker-room, referencing employees by ID from the shared roster instead of storing names directly in the locker document."
    />
  );
}
